updated: 04/04/26

TODO (done.) Write documentation underneath "docs/" folder

TODO Work in peer functionality.
    TODO Add a better way of identifying peers that is NOT just <ip>:<port>.
        NOTE: Something like tcp:///<ip>:<port>/<UUID>
    TODO (done.) Add peer discovery (including handshakes for connecting with other peers)

TODO Gut the entire account system. 
     NOTE: We will still have external & contract accounts. 
     NOTE: Users will not have to specify --type <external|contract>. 
     NOTE: If a user is creating an account, we will assume it is external.
     NOTE: That said, if contract bytecode is later set on said account, it CAN become 
           a contract account (but only if / when the contract bytecode becomes present)