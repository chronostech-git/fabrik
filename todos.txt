updated: 04/05/2026 @ 1:43am

Changes logged here...

- Consensus changed and heavily developed yesterday and this morning.
- consensus.go > interface(s) added for the consensus engine
- params.go > chain parameters added (+2)
- Genesis (still seperate type), can now be easily converted to a block type using ToBlock()
- Added gas metrics to the BlockHeader type
- chain command > [stricter] - creates coinbase transaction and the genesis block
    * .\cli\chain --datadir <datadir> --gaslimit <gaslimit>
    * NOTE: --gaslimit specifies the limit for the genesis block
- Chain functions heavily changed / developed upon
    * Added ChainIterator 
    * Added ConsensusEngine interface functions 
    * Removed underdeveloped functions, messy functions, and non-used functions
    * Instead of applying the genesis block to the state seperately, it applies it in 
      the (*chain).NewWithGenesis(...) function. This makes the "apply genesis block" function
      private, as it is not used elsewhere.
    * (*block).(*header).Height now stores the correct height. Before it would stay at 0.  
