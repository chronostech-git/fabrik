03/31/26

TODO (done.): Combine cmd/genesis with cmd/chain. 
    NOTE: Genesis should be generated by the cli/chain command if there is no existing chain
    NOTE: The cmd/node folder should be handling P2P/Networking logic--which may indirectly use blockchain logic.
          However, the cmd/node folder should not solely & directly call blockchain functions. That is meant for cmd/chain

TODO (done.): Remove wallet generation, combine it with cmd/accounts. 
    NOTE: Create a wallet on account generation if <datadir>/keystore does not exist
    NOTE: If the supplied data directory DOES exist, and has keystore/<address>.key, then Create
          the account using that key

TODO (done.): Add comments to code where nessecary

TODO (done.): Create a CONTRIBUTING.md file for contributors to follow

TODO (done.): Add a formatting script under scripts/format.sh (mostly for contributers).

TODO: Begin working on the P2P folder in /internal